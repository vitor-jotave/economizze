export type NotificationCenterItem = {
    id: string;
    title: string;
    time: string;
};

export type NotificationCenterActivity = {
    id: string;
    title: string;
    time: string;
    tone: string;
};
