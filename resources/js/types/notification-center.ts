export type NotificationCenterItem = {
    id: string;
    title: string;
    body: string;
    time: string;
    tone: string;
};

export type NotificationCenterActivity = {
    id: string;
    title: string;
    time: string;
    tone: string;
};
